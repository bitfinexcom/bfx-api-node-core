'use strict'

/**
 * An object passed to all plugin method handlers
 *
 * @typedef {object} module:bfx-api-node-core.PluginHelpers
 * @property {Function} emit - emit a plugin event automatically including the
 *   plugin ID
 * @property {Function} getState - returns the current plugin state
 */
